name: xvec
channels:
  - conda-forge
dependencies:
  - python=3.10
  # required
  - shapely >=2
  - xarray
  - rioxarray
  - joblib
  - rasterio
  - tqdm
  - pyproj
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest-reportlog
  - geopandas-base
  - geodatasets
  - pyogrio

